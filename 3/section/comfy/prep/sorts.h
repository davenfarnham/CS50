// header for bubble sort
void bubble (int* array, int size);

// header for selection sort
void selection (int* array, int size);

// header for insertion sort
void insertion(int* array, int size);

// header for function in merge
int* explode(int* array, int size);

// header for function in merge
int* combine (int* left, int s1, int* right, int s2);

// swap helper
void swap(int* a, int* b);

// create an array of random size
int* ran_array(int size);
